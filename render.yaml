services:
  - type: web
    name: student-performance-ml
    runtime: python
    buildCommand: pip install -r requirements.txt && python src/components/data_ingestion.py
    startCommand: gunicorn app:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
